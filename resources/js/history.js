import { createBrowserHistory } from 'history';
// Handle browser history in order to track visited pages. This will redirect to previous page when we clock back button.
export default createBrowserHistory();